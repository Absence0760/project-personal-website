{
  "name": "project-personal-website",
  "private": true,
  "type": "module",
  "description": "Personal website at jaredhoward.com — SvelteKit static site deployed to GitHub Pages.",
  "scripts": {
    "//-- dev --": "SvelteKit dev server (http://localhost:7777) and a static preview (http://localhost:8888) of the built site.",
    "dev": "vite dev --port 7777",
    "preview": "vite preview --port 8888",
    "//-- build --": "Production static build → ./build (adapter-static, full prerender), then build:pdf renders build/cv.pdf from the built /cv/ page (needs `pnpm exec playwright install chromium` once). deploy.yml runs both before uploading to GitHub Pages.",
    "build": "vite build",
    "build:pdf": "node scripts/generate_cv_pdf.mjs",
    "//-- check --": "Type + Svelte diagnostics. svelte-kit sync first so generated types (.svelte-kit/) exist.",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "//-- test --": "Vitest unit suite; test:scripts validates this file's script layout (scripts/check_root_scripts.mjs).",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:scripts": "node scripts/check_root_scripts.mjs"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "3.0.10",
    "@sveltejs/kit": "2.70.1",
    "@sveltejs/vite-plugin-svelte": "7.2.0",
    "@types/node": "26.1.1",
    "playwright": "1.62.0",
    "svelte": "5.56.8",
    "svelte-check": "4.7.3",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  },
  "pnpm": {
    "overrides": {
      "cookie@<0.7.0": "^0.7.0"
    }
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.2"
}
